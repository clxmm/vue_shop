server:
  port: 9009


spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/vueshop?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource

mapper:
  mappers:
    - tk.mybatis.mapper.common.Mapper
  not-empty: false
  identity: MYSQL

logging:
  level:
    cn.ehanghai.zsaj: debug